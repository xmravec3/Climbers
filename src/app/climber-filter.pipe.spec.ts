import { ClimberFilterPipe } from './climber-filter.pipe';

describe('ClimberFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClimberFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
